# API Integration & Data Models Guide

В этом документе собраны правила интеграции фронтенда и бэкенда, а также детали реализации конкретных сущностей на основе существующих эндпоинтов.

## 1. Разработка новых API Endpoints

При написании новых роутов на бэкенде, строго следуйте устоявшейся структуре ответов:

- **Успешный ответ (200/201):** Должен возвращать либо чистый массив объектов (для списков), либо JSON-объект, где данные лежат прямо в корне (например, возвращается сам объект ресурса или объект через вложенный ключ вроде `{ user: {...}, token: "..." }` для авторизации).
- **Ошибки (4xx/500):** Всегда возвращают объект с полем `message`: `{ "message": "Описание ошибки" }`. Бэкенд реализован с применением стандартных NestJS Exceptions (`BadRequestException`, `NotFoundException`, `InternalServerErrorException`), которые сами формируют правильный JSON.

## 2. Сущности и их особенности

### Тренировки (Events)

События (`Event`) разделяются на два типа статусов:

- `FUTURE` — запланированная тренировка.
- `COMPLETED` / `PAST` — прошедшая тренировка.
  У события обязательно есть `eventDate`, `scheme` (`FOR_TIME`, `AMRAP`, `EMOM` и др.) и владелец `userId`. Событие может быть привязано к команде `teamId`.
  Массив `exercises` сохраняется как JSON и строго валидируется через `ExerciseDto`. Начиная с недавних обновлений, помимо стандартных весов и повторов (`rxWeight`, `rxReps`), он поддерживает опциональные поля для времени, калорий и дистанции (`rxTime`, `rxDistance`, `rxCalories`, и их SC-эквиваленты).

### Результаты тренировок (EventResult)

- Привязаны к конкретному `Event` через `eventId`.
- Содержат поля `time` (строка, например "10:30"), `value` (число, например количество повторений), `scaling` (модификатор: "RX", "SCALED").
- На бэкенде контроллеры для результатов часто вкладываются в путь родителя: `POST /events/:eventId/results`.

### Команды (Teams)

- Команда имеет `ownerId` (тренер, создавший ее).
- Связь пользователей с командой осуществляется через промежуточную таблицу `TeamMember`, где указывается роль юзера внутри команды (`MEMBER`, `ADMIN`, `OWNER`).
- Для приглашения атлетов в команду используется уникальный `inviteCode`. Эндпойнт инвайта должен проверять валидность кода и добавлять связь в `TeamMember`.

## 3. Интеграция с Frontend (Связки)

1. Не ходите напрямую через `fetch` в UI-компонентах. Всю логику общения с API необходимо выносить в специальные модули внутри `frontend/src/lib/api/` (например `authApi`, `eventsApi`, `teamApi`).
2. Эти API модули затем вызываются либо из Zustand сторов для глобальных состояний, либо через React Server Components / Client fetcher-ы в связке с хуками загрузки для локальных данных.
3. Обязательно подставляйте авторизационный токен (обычно `Bearer JWT`) в заголовки запросов, если API-клиент не настроен делать это автоматически с помощью axios/interceptor-ов.

## 4. Обработка истёкшей сессии (Auth Session Expiry)

`apiClient` (`frontend/src/lib/api/apiClient.ts`) автоматически обрабатывает 401:

1. При первом 401 — пытается обновить access_token через `POST /api/auth/refresh`.
2. Если refresh тоже 401/403 — вызывает `forceLogout()`.

### Что делает `forceLogout()`

```
POST /api/auth/logout          ← отзываем refresh_token на бэкенде
window.dispatchEvent("auth:session-expired")  ← глобальный browser event
useAuthStore.getState().logout()              ← сброс Zustand in-memory state
```

### Компонент `AuthSessionWatcher`

Расположен в `frontend/src/components/ui/AuthSessionWatcher.tsx`.  
Монтируется **один раз** в `app/layout.tsx` внутри `<ToastProvider>`.

При получении события `auth:session-expired`:

- Показывает `error` toast «Сессия истекла. Пожалуйста, войдите снова.»
- Через 300мс делает `router.push('/login')` — это закрывает все открытые модалы и сбрасывает локальный UI-стейт.

### Правило для новых компонентов

**Не** добавляйте отдельную обработку 401 в каждый компонент или форму.  
Вся логика централизована в `apiClient` → `AuthSessionWatcher`. Компоненты просто пробрасывают ошибки выше через `throw`.

## 5. Защита от состояния гонки при обновлении токена (Race Conditions)

Добавлен механизм синхронизации запросов на обновление токена (`refresh_token`) между вкладками:

- Использование Web Locks API (`navigator.locks.request("auth_refresh")`).
- Использование `localStorage` (`auth_last_refresh`) для отсечения лишних запросов после успешного обновления из другой вкладки.
  Это предотвращает отзыв токенов при одновременных вызовах `apiClient` из разных вкладок.

## 6. CSRF и настройки Cookies

- **SameSite=Strict:** `access_token` и `refresh_token` теперь устанавливаются с `SameSite=Strict` для повышенной защиты от CSRF.
- **HttpOnly & Secure:** Токены остаются недоступными для JS (кроме старых вызовов) и требуют HTTPS в production-окружении (`isProduction`).
- **X-CSRF-Token:** Для всех мутирующих запросов (`POST`, `PUT`, `PATCH`, `DELETE`) по-прежнему отправляется `X-CSRF-Token` в заголовке, что вместе с `SameSite=Strict` обеспечивает двойную защиту.

## 7. Hydration и мигание UI (Zustand)

Чтобы избежать показа кэшированного приватного UI (мелькания старого состояния) на медленном интернете перед тем, как бэкенд (через `apiClient`) обнаружит, что токен недействителен, применяется следующий подход в `useAuthStore`:

- Если при загрузке страницы в `localStorage` (или куки) есть `isAuthenticated` и `user`, мы **не** снимаем состояние начальной загрузки (`isLoading = false`) сразу.
- Запускается асинхронная проверка `verifyUser()`, и состояние загрузки сбрасывается (`useAuthStore.setState({ isLoading: false })`) только по её завершении при помощи `.finally()`.
- Таким образом, пользователь будет видеть сплэш-экран (или лоадер) ровно столько времени, сколько необходимо на подтверждение сессии. Если сессия протухла, пользователя перенаправит на страницу входа ещё до того, как загрузится защищенный интерфейс.

## 8. Streaming Responses (Next.js Proxy)

Для предотвращения высокого потребления памяти сервером Next.js при проксировании запросов с большими объемами данных (например, списки тренировок) `BackendClient` использует **Streaming Responses**.

Вместо буферизации данных:

```typescript
// ПЛОХО: Аллокация памяти на размер всего ответа
const data = await response.json()
return NextResponse.json(data)
```

Мы передаем напрямую `body` стрим от fetch:

```typescript
// ХОРОШО: Next.js проксирует стрим напрямую
return new NextResponse(response.body, {
  /* заголовки */
})
```

Эта практика обязательна для любых новых универсальных API хелперов на стороне Server-Side.

## 9. Пагинация и Infinite Scroll

Все эндпоинты, возвращающие потенциально большие списки данных (сущности пользователей, тяжелые записи тренировок `exercises`, `events`), **обязаны** поддерживать пагинацию `skip/take` на бэкенде.

- **Бэкенд:** Принимает опциональные параметры `@Query('page')` и `@Query('limit')`. Если параметры переданы, возвращает формат `{ data: Entity[], meta: { hasMore: boolean } }`.
- **Фронтенд:** Если возвращается большой массив, в UI-компонентах (например в `DashboardPage`) должна реализовываться логика накапливания стейта и дозагрузка через IntersectionObserver (Infinite Scroll). Загружаемые страницы должны отфильтровывать возможные дубликаты перед добавлением в стейт.
  121:
  122: ## 10. Стабильность соединений и предотвращение циклов рендеринга
  123:
  124: Чтобы избежать "бесконечных перезагрузок" и спама WebSocket-подключений (особенно при нестабильном бэкенде), следуйте этим правилам:
  125:
  126: ### Стабильные зависимости в эффектах
  127: **Никогда** не ставьте весь объект `user` или `state` в зависимости `useEffect`, если вам нужно только одно поле.
  128:
  129: `tsx
130: // ПЛОХО: Перезапускает сокет при ЛЮБОМ изменении профиля (даже если ID тот же)
131: useEffect(() => { ... }, [user])
132: 
133: // ХОРОШО: Перезапускает только при смене аккаунта
134: useEffect(() => { ... }, [user?.id])
135: `
  136:
  137: ### Предотвращение лишних обновлений Zustand
  138: Если API возвращает данные, которые могут быть идентичны текущим, делайте проверку перед вызовом `set()`. Это предотвращает каскадные ререндеры зависимых компонентов.
  139:
  140: `typescript
141: // Внутри стора
142: const currentData = JSON.stringify(get().data)
143: const newData = JSON.stringify(response.data)
144: if (currentData !== newData) {
145:   set({ data: response.data })
146: }
147: `
  148:
  149: ### Безопасные редиректы
  150: Избегайте использования `window.location.href` для внутренних переходов, так как это вызывает полную перезагрузку приложения и потерю стейта. Используйте `router.push()` / `router.replace()` из `next/navigation`.
  151:
  152: - **Исключение:** Жесткая перезагрузка допустима только при полном сбросе авторизации или серьезных системных сбоях, где требуется очистка кэша браузера.
  153:
